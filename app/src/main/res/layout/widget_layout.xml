<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    android:paddingTop="12dp"
    android:paddingBottom="10dp">

    <!-- Header row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="8dp">

        <ImageButton
            android:id="@+id/widget_obsidian_button"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:src="@drawable/obsidian_logo"
            android:background="@null"
            android:contentDescription="@string/open_obsidian"
            android:scaleType="centerInside"
            android:layout_marginEnd="8dp" />

        <TextView
            android:id="@+id/widget_header_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="#tag"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#845AD6" />

        <ImageButton
            android:id="@+id/widget_search_button"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@drawable/ic_search_grey"
            android:background="@null"
            android:contentDescription="@string/search"
            android:scaleType="centerInside" />

        <ImageButton
            android:id="@+id/widget_refresh_button"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@drawable/ic_refresh_grey"
            android:background="@null"
            android:contentDescription="@string/refresh"
            android:scaleType="centerInside"
            android:layout_marginStart="2dp" />

    </LinearLayout>

    <!-- Purple divider -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#2D2D4E"
        android:orientation="horizontal"
        android:layout_marginBottom="4dp" />

    <!-- Loading spinner (shown while data loads) -->
    <FrameLayout
        android:id="@+id/widget_loading_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="visible">
        <ProgressBar
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:indeterminate="true"
            android:indeterminateTint="#845AD6" />
    </FrameLayout>

    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:scrollbars="none"
        android:visibility="gone" />

    <TextView
        android:id="@+id/widget_empty_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="@string/no_notes"
        android:textSize="13sp"
        android:textColor="#555570" />

</LinearLayout>
